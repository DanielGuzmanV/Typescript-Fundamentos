/// Ejemplos con el operador ternario:
// Ejemplo 1:

let nombre1: string = 'Paolo';
let edad1: number = 34;

let acceso1 = edad1 > 18 ? 'Puede ingresar' : 'No puede ingresar';
console.log(`El usuario: ${nombre1} ${acceso1}`);
console.log('===================================================')

// Ejemplo 2:
let nombre2: string = 'Barrientos';
let edad2: number = 17;

let acceso2 = edad2 > 18 ? 'Puede ingresar' : 'No puede ingresar';
console.log(`El usuario: ${nombre2} ${acceso2}`);

// El ternario tiene esta estructura
// Expresion que evalua ? "Si es true" : "Si es false";

